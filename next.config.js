/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  images: {
    domains: ["image.tmdb.org", "via.placeholder.com", "flagcdn.com"],
  },
};
